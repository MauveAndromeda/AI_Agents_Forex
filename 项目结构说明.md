# 📁 项目结构说明

## 🎯 核心文件（必需）

### 主要入口脚本
```
一键安装运行.py           ⭐ 推荐：自动安装依赖并运行
run_backtest.py            统一菜单入口
真实回测_5年增强版.py      5年真实数据回测（推荐）
一键回测_高速版.py         高速并发版本
一键回测完整版.py          标准90天回测
check_api_key.py          API密钥诊断工具
```

### 配置文件
```
requirements.txt          Python依赖列表
.env                      API密钥配置（需自己创建）
```

### 文档
```
README.md                 英文说明
README_简体中文.md         中文完整文档
快速开始.md                5分钟快速指南
使用说明_ZIP下载用户.md     ZIP用户专用指南
项目结构说明.md            本文件
GPT-5-nano配置指南.md      GPT-5配置详解
```

---

## 📦 核心代码目录

### src/ - 核心功能模块

```
src/
├── __init__.py
│
├── data/                  # 数据连接器
│   ├── __init__.py
│   ├── mt5_connector.py          MT5实时数据
│   └── simulated_data_generator.py   模拟数据生成器
│
├── models/                # Alpha因子和模型
│   ├── __init__.py
│   ├── forex_alpha_factors.py         100+ Alpha因子
│   └── forex_alpha_factors_optimized.py   优化版因子
│
├── ai/                    # LLM客户端
│   ├── __init__.py
│   └── unified_llm_client.py     多提供商LLM客户端
│
├── agents/                # AI Agents
│   ├── __init__.py
│   ├── trading_agents.py             Multi-Agent系统
│   ├── market_microstructure.py      市场微观结构分析
│   └── ai_enhancements_2025.py       强化学习+自适应学习
│
├── risk/                  # 风险管理
│   ├── __init__.py
│   ├── forex_risk_manager.py         标准风险管理
│   └── high_leverage_risk_manager.py  高杠杆专用
│
├── strategy/              # 交易策略
│   ├── __init__.py
│   └── forex_strategy.py
│
├── execution/             # 执行引擎
│   ├── __init__.py
│   └── mt5_executor.py
│
└── backtest/              # 回测引擎
    ├── __init__.py
    └── backtest_engine.py
```

---

## 🛠️ 辅助目录

### tools/ - 工具脚本
```
tools/
├── system_validator.py       系统验证工具
└── visualize_results.py      结果可视化
```

### tests/ - 测试文件
```
tests/
└── test_mt5_connector.py     MT5连接测试
```

### backtest_results/ - 结果输出
```
backtest_results/
├── .gitkeep                  占位文件
├── *.json                    回测结果（运行后生成）
└── *.csv                     权益曲线（运行后生成）
```

---

## 🚫 不需要的文件（已删除）

这些文件在旧版本中存在，但现在已经删除：

```
❌ config.py              旧配置文件（已整合）
❌ run_live_strategy.py   实盘交易（回测不需要）
❌ setup.py               打包脚本（不需要）
```

---

## 📊 文件统计

| 类别 | 数量 | 说明 |
|------|------|------|
| 核心脚本 | 6个 | 各种回测版本 |
| 源代码模块 | 15+ | src/ 目录下 |
| 文档 | 7个 | 各种说明文档 |
| 工具 | 2个 | 辅助工具 |
| 总行数 | 8,000+ | 核心代码 |

---

## ✅ 完整性检查

下载ZIP后，应该包含以下关键目录：

```bash
# 检查命令（PowerShell）
Test-Path src         # 应该返回 True
Test-Path src/data    # 应该返回 True
Test-Path src/models  # 应该返回 True
Test-Path src/ai      # 应该返回 True
Test-Path src/agents  # 应该返回 True
Test-Path src/risk    # 应该返回 True
```

如果任何一个返回 False，说明 ZIP 下载不完整！

---

## 🎯 快速开始

1. **确认目录完整**
   ```bash
   dir src
   ```
   应该看到：data, models, ai, agents, risk

2. **运行自动安装**
   ```bash
   python 一键安装运行.py
   ```

3. **选择模式开始回测**

---

## 💡 常见问题

### Q: 为什么 src/ 目录很重要？

**A**: 所有核心功能都在 src/ 里！没有它就无法运行。

### Q: 哪些文件必须手动创建？

**A**: 只有 `.env` 文件（存放API密钥），其他都已包含。

### Q: backtest_results/ 为什么是空的？

**A**: 正常！运行回测后会自动生成结果文件。

---

**最后更新**: 2025-01-07
