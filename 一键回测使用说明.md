# AI Agents Forex - 一键回测完整版使用说明

## 📋 文件说明

**文件名**: `一键回测完整版.py`

这是一个完整的、自动化的外汇交易回测系统，集成了 8 大 AI 组件，支持一键运行，自动安装所有依赖。

---

## ✨ 功能特性

### 8 大 AI 组件

1. **Alpha 因子引擎** - 100+ 量化因子
2. **Multi-Agent System** - 4 个专业 Agent
   - Technical Analyst Agent (技术分析)
   - Risk Manager Agent (风险管理)
   - Sentiment Analyst Agent (情绪分析)
   - Execution Agent (执行决策)
3. **Market Microstructure Analyzer** - 零售 vs 机构订单流分析
4. **Reinforcement Learning Agent** - Q-learning 强化学习
5. **Advanced LLM Agent** - Chain-of-Thought 推理
6. **Adaptive Learning System** - 自适应学习系统
7. **High Leverage Risk Manager** - 50x 专用风险管理
8. **Multi-Timeframe Analysis** - 多时间框架分析

### 核心特点

✅ **自动安装依赖** - 无需手动安装任何库
✅ **支持多种 LLM** - GPT-5-nano, GPT-4o, Claude Sonnet 4.5, DeepSeek, Gemini
✅ **模拟数据支持** - 无需 MetaTrader 5 即可运行
✅ **详细回测报告** - JSON + CSV 格式
✅ **中文界面** - 完整中文提示和说明
✅ **生产级质量** - 完整错误处理和日志记录

---

## 🚀 快速开始

### 方式 1: 使用默认设置（推荐新手）

```bash
python 一键回测完整版.py
```

默认配置：
- 交易对: EURUSD, GBPUSD
- 初始资金: $10,000
- 杠杆: 50x
- LLM: gpt-5-nano
- 回测天数: 90 天

### 方式 2: 指定 LLM 模型

```bash
# 使用 Claude Sonnet 4.5
python 一键回测完整版.py --llm claude-sonnet-4.5

# 使用 GPT-4o
python 一键回测完整版.py --llm gpt-4o

# 使用 DeepSeek
python 一键回测完整版.py --llm deepseek
```

### 方式 3: 自定义所有参数

```bash
python 一键回测完整版.py \
  --llm claude-sonnet-4.5 \
  --symbols EURUSD GBPUSD USDJPY \
  --balance 50000 \
  --leverage 100 \
  --days 180
```

### 方式 4: 使用真实 MT5 数据（需要 MT5）

```bash
python 一键回测完整版.py --real-data
```

---

## 📊 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `--llm` | LLM 提供商 | gpt-5-nano | `--llm claude-sonnet-4.5` |
| `--symbols` | 交易对列表 | EURUSD GBPUSD | `--symbols EURUSD USDJPY` |
| `--balance` | 初始资金 | 10000 | `--balance 50000` |
| `--leverage` | 杠杆倍数 | 50 | `--leverage 100` |
| `--days` | 回测天数 | 90 | `--days 180` |
| `--api-key` | API 密钥 | 从环境变量读取 | `--api-key sk-xxx` |
| `--real-data` | 使用真实 MT5 数据 | False | `--real-data` |

### 支持的 LLM

- `gpt-5-nano` (默认，最快)
- `gpt-4o-mini`
- `gpt-4o`
- `gpt-4`
- `claude-sonnet-4.5` (推荐)
- `claude-3.5-sonnet`
- `claude-3-opus`
- `deepseek`
- `gemini`
- `gemini-pro`

---

## 🔑 API 密钥设置

### 方法 1: 环境变量（推荐）

```bash
# OpenAI
export OPENAI_API_KEY="sk-your-key-here"

# Anthropic (Claude)
export ANTHROPIC_API_KEY="sk-ant-your-key-here"

# Google (Gemini)
export GOOGLE_API_KEY="your-key-here"
```

### 方法 2: .env 文件

创建 `.env` 文件：

```bash
OPENAI_API_KEY=sk-your-key-here
ANTHROPIC_API_KEY=sk-ant-your-key-here
GOOGLE_API_KEY=your-key-here
```

### 方法 3: 命令行参数

```bash
python 一键回测完整版.py --api-key sk-your-key-here
```

---

## 📈 回测结果

### 输出文件

回测完成后，会在 `backtest_results/` 目录生成以下文件：

1. **backtest_YYYYMMDD_HHMMSS.json** - 完整回测结果（JSON 格式）
2. **trades_YYYYMMDD_HHMMSS.csv** - 交易明细（CSV 格式）
3. **equity_curve_YYYYMMDD_HHMMSS.csv** - 权益曲线（CSV 格式）
4. **backtest.log** - 详细日志文件

### 结果示例

```
================================================================================
🎯 回测结果 - AI AGENTS FOREX 完整版
================================================================================
初始资金:                      $10,000.00
最终权益:                      $12,450.00
总盈亏:                         $2,450.00
总回报率:                           24.50%
--------------------------------------------------------------------------------
交易总数:                               45
盈利交易:                               28
亏损交易:                               17
胜率:                                62.2%
--------------------------------------------------------------------------------
平均盈利:                          $180.50
平均亏损:                          $-95.20
盈亏比:                                2.15
Sharpe 比率:                           1.85
最大回撤:                             8.5%
--------------------------------------------------------------------------------
盈利天数:                               12
亏损天数:                                8
日均盈亏:                          $122.50
--------------------------------------------------------------------------------
🤖 AI 组件分析:
平均置信度:                           68.5%
Alpha 因子:                            0.245
零售情绪 (反向):                      -0.125
机构订单流 (跟随):                     0.312
================================================================================
```

---

## 🌐 支持的运行环境

### ✅ 已测试环境

- **本地 Windows/Mac/Linux** - Python 3.8+
- **vast.ai PyTorch + Jupyter** - GPU 云环境
- **Google Colab** - 免费云环境
- **Kaggle Notebooks** - 数据科学平台
- **Azure ML / AWS SageMaker** - 企业云平台

### 📦 依赖库（自动安装）

核心依赖：
- pandas
- numpy
- scipy
- python-dotenv
- python-dateutil

AI/ML 库：
- openai
- anthropic
- google-generativeai

技术分析：
- TA-Lib（可选，如果安装失败会使用内置方法）

---

## 🎯 使用场景

### 场景 1: 快速验证策略

```bash
# 使用模拟数据快速测试 30 天
python 一键回测完整版.py --days 30
```

### 场景 2: 深度回测

```bash
# 180 天深度回测，使用 Claude
python 一键回测完整版.py \
  --llm claude-sonnet-4.5 \
  --days 180 \
  --symbols EURUSD GBPUSD USDJPY AUDUSD
```

### 场景 3: 高杠杆测试

```bash
# 测试 100x 杠杆的表现
python 一键回测完整版.py \
  --leverage 100 \
  --balance 5000
```

### 场景 4: 大资金测试

```bash
# 模拟 $100,000 账户
python 一键回测完整版.py \
  --balance 100000 \
  --days 365
```

---

## 🔧 高级功能

### 1. 查看详细日志

```bash
tail -f backtest.log
```

### 2. 分析权益曲线

使用 Python 读取权益曲线：

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取权益曲线
equity = pd.read_csv('backtest_results/equity_curve_20250106_120000.csv')
equity['time'] = pd.to_datetime(equity['time'])

# 绘制图表
plt.figure(figsize=(12, 6))
plt.plot(equity['time'], equity['equity'])
plt.title('Equity Curve')
plt.xlabel('Time')
plt.ylabel('Equity ($)')
plt.grid(True)
plt.show()
```

### 3. 分析交易明细

```python
import pandas as pd

# 读取交易记录
trades = pd.read_csv('backtest_results/trades_20250106_120000.csv')

# 按交易对分组分析
by_symbol = trades.groupby('symbol').agg({
    'pnl_usd': ['sum', 'mean', 'count'],
    'outcome': lambda x: (x == 'win').sum() / len(x)
})

print(by_symbol)
```

---

## ⚠️ 常见问题

### Q1: TA-Lib 安装失败怎么办？

A: 脚本会自动检测，如果 TA-Lib 安装失败，会使用内置的技术指标计算方法，不影响回测。

### Q2: 没有 API 密钥可以运行吗？

A: 可以！系统会自动切换到纯量化模式，仍然使用 Multi-Agent System 和 Alpha 因子，只是不使用 LLM 推理功能。

### Q3: 需要 MetaTrader 5 吗？

A: 不需要！默认使用高质量的模拟数据生成器。如果需要使用真实数据，可以添加 `--real-data` 参数。

### Q4: 可以在 Jupyter Notebook 中运行吗？

A: 可以！在 Notebook 中运行：

```python
!python 一键回测完整版.py --days 30
```

### Q5: 回测速度慢怎么办？

A: 可以减少回测天数或减少交易对数量：

```bash
python 一键回测完整版.py --days 30 --symbols EURUSD
```

---

## 📚 技术架构

### 信号生成流程

```
1. 数据获取 (Multi-Timeframe)
   ↓
2. Alpha 因子计算 (100+ factors)
   ↓
3. Multi-Agent System 基础分析
   ├─ Technical Analyst (技术分析)
   ├─ Risk Manager (风险评估)
   ├─ Sentiment Analyst (情绪分析)
   └─ Execution Agent (执行决策)
   ↓
4. 2025 AI 增强
   ├─ Market Microstructure (订单流分析)
   ├─ Reinforcement Learning (Q-learning)
   ├─ Advanced LLM (Chain-of-Thought)
   └─ Adaptive Learning (自适应学习)
   ↓
5. 信号整合 (加权投票)
   ↓
6. 高杠杆风险管理
   ↓
7. 交易执行
```

### 权重分配

- Multi-Agent 基础决策: **40%**
- Alpha 因子: **20%**
- 机构订单流: **20%**
- 强化学习: **10%**
- LLM 推理: **10%**
- 自适应学习: **信心度乘数**

---

## 🎓 学习资源

### 理解 Alpha 因子

查看 `src/models/forex_alpha_factors_optimized.py` 了解 100+ 因子的实现。

### 理解 Multi-Agent System

查看 `src/agents/trading_agents.py` 了解 4 个 Agent 的决策逻辑。

### 理解强化学习

查看 `src/agents/ai_enhancements_2025.py` 了解 Q-learning 实现。

---

## 📞 支持

如有问题，请查看：

1. **日志文件**: `backtest.log`
2. **项目文档**: `README.md`
3. **代码注释**: 所有代码都有详细中文注释

---

## 📄 许可证

本项目仅供学习和研究使用。实盘交易风险自负。

---

## 🎉 开始使用

```bash
# 立即开始第一次回测！
python 一键回测完整版.py

# 祝交易顺利！ 🚀
```

---

**最后更新**: 2025-01-06
**版本**: 1.0.0
**作者**: AI Agents Forex Team
