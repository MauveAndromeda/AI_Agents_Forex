# 🚀 快速开始指南

**5 分钟学会使用本系统**

---

## 📦 第一步：下载项目

### 方式 1: ZIP 下载（推荐，最简单）

1. 打开 GitHub 项目页面
2. 点击绿色的 **Code** 按钮
3. 选择 **Download ZIP**
4. 解压到任意目录（比如 `C:\Users\你的用户名\Desktop\AI_Agents_Forex`）

### 方式 2: Git Clone

```bash
git clone https://github.com/MauveAndromeda/AI_Agents_Forex.git
cd AI_Agents_Forex
```

---

## ⚙️ 第二步：安装依赖

打开命令行（PowerShell、CMD 或终端），进入项目目录：

```bash
# Windows
cd C:\Users\你的用户名\Desktop\AI_Agents_Forex

# 安装依赖
pip install -r requirements.txt
```

**如果不想手动安装**，脚本会自动检测并安装缺失的依赖。

---

## 🎯 第三步：运行回测

### 最简单的方式（推荐）

```bash
python run_backtest.py
```

然后你会看到菜单：

```
AI Agents Forex - 智能外汇交易回测系统
════════════════════════════════════════════════════════════════

欢迎！请选择回测模式：

  [1] 🚀 真实回测 5 年增强版（推荐）
      • 支持真实 MT5 历史数据
      • 智能自适应学习系统
      • 详细调试日志
      • 最多 5 年数据回测

  [2] ⚡ 高速并发回测版
      • 并发 API 调用（10-100倍速）
      • 智能缓存系统
      • 支持纯量化模式

  [3] 📊 标准回测版
      • 完整 8 组件系统
      • 适合快速测试
      • 90 天回测

  [4] 🔍 检查 API 密钥配置
      • 查看 API 密钥位置
      • 诊断配置问题

  [0] 退出

请选择 [0-4]:
```

**输入 `1` 然后回车**，即可开始回测！

---

## 🔑 API 密钥配置（第一次运行需要）

### 情况 1: 系统自动提示

第一次运行时，如果没有检测到 API 密钥，会提示：

```
未检测到 OPENAI_API_KEY

💡 请选择:
   [1] 粘贴 API 密钥（仅本次使用）
   [2] 粘贴并保存到 .env 文件（推荐）
   [3] 跳过（使用纯量化模式，不调用 LLM）
```

**推荐选择 [2]**，这样下次就不用再输入了。

### 情况 2: 手动配置

在项目根目录创建 `.env` 文件：

```bash
# Windows (PowerShell)
echo "OPENAI_API_KEY=sk-你的密钥" > .env

# 或者用记事本
notepad .env
```

然后输入：

```
OPENAI_API_KEY=sk-proj-你的OpenAI密钥
```

保存即可。

### 情况 3: 没有 API 密钥？

**不用担心！** 选择 [3] 跳过，系统会使用纯量化模式（不调用 LLM API，完全免费）。

---

## 📊 查看结果

回测完成后，结果会保存在 `backtest_results/` 目录：

```
backtest_results/
├── backtest_5y_20251107_162202.json       # 详细数据
└── equity_curve_5y_20251107_162202.csv    # 权益曲线
```

同时会在终端显示完整报告：

```
回测结果 - 5 年
════════════════════════════════════════════════════════════════
总交易数:      45
  ├─ 盈利:     27 (60.0%)
  └─ 亏损:     18

资金变化:
  ├─ 初始:     $10,000.00
  ├─ 最终:     $15,230.50
  └─ 收益率:   52.3%

交易表现:
  ├─ 平均盈利: $250.30
  ├─ 平均亏损: $180.20
  ├─ 盈亏比:   1.39
  └─ 利润因子: 2.15

风险指标:
  ├─ 最大回撤: 12.5%
  └─ 夏普比率: 1.85
════════════════════════════════════════════════════════════════
```

---

## 🎓 进阶用法

### 自定义参数

```bash
# 指定回测年限
python 真实回测_5年增强版.py --years 3

# 指定交易对
python 真实回测_5年增强版.py --symbols EURUSD GBPUSD USDJPY

# 指定初始资金和杠杆
python 真实回测_5年增强版.py --balance 50000 --leverage 50

# 开启调试模式（查看详细日志）
python 真实回测_5年增强版.py --debug

# 组合使用
python 真实回测_5年增强版.py --years 5 --symbols EURUSD GBPUSD --balance 20000 --debug
```

### 使用不同的 LLM 模型

```bash
# 使用 GPT-4o-mini（更便宜）
python 真实回测_5年增强版.py --llm gpt-4o-mini

# 使用 GPT-4o（更强大）
python 真实回测_5年增强版.py --llm gpt-4o
```

### 高速并发模式

```bash
# 第一次：建立缓存
python 一键回测_高速版.py --mode cache

# 之后：使用缓存（10-30倍速）
python 一键回测_高速版.py --mode fast

# 或者：纯量化（100倍速）
python 一键回测_高速版.py --mode ultra-fast
```

---

## 🐛 遇到问题？

### 问题 1: "没有执行任何交易"

**解决方案**：
- 确保运行的是 **真实回测 5 年增强版**
- 通过 `run_backtest.py` 选择 [1]
- 或直接运行：`python 真实回测_5年增强版.py --debug`

### 问题 2: "找不到 src 模块"

**解决方案**：
- 确保在项目根目录运行
- 检查当前目录：`pwd` 或 `cd`
- 应该能看到 `src/` 文件夹

### 问题 3: "API 调用失败"

**解决方案**：
```bash
# 检查 API 密钥配置
python check_api_key.py

# 或通过统一入口
python run_backtest.py  # 选择 [4]
```

### 问题 4: 其他错误

```bash
# 开启调试模式查看详细错误
python 真实回测_5年增强版.py --debug
```

---

## 💡 推荐的学习路径

### 第 1 天：基础回测

```bash
# 运行标准回测（快速体验）
python run_backtest.py  # 选择 [3]
```

### 第 2 天：真实数据回测

```bash
# 运行 5 年回测
python run_backtest.py  # 选择 [1]
```

### 第 3 天：参数调优

```bash
# 尝试不同参数
python 真实回测_5年增强版.py --years 3 --symbols EURUSD
python 真实回测_5年增强版.py --years 5 --symbols GBPUSD USDJPY
```

### 第 4 天：高速测试

```bash
# 建立缓存
python 一键回测_高速版.py --mode cache

# 快速迭代测试
python 一键回测_高速版.py --mode fast
```

---

## 📚 下一步

- 阅读 `README_简体中文.md` 了解详细功能
- 查看 `src/` 目录了解代码结构
- 修改参数进行更多实验
- 查看 `backtest_results/` 分析结果

---

**就是这么简单！** 🎉

如果还有问题，请在 GitHub 上提交 Issue。
